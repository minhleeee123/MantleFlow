{
    "name": "trading-bot-contract",
    "version": "1.0.0",
    "description": "Auto-trading smart contract for Mantle Sepolia Testnet",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy": "hardhat run scripts/deploy.js --network mantleSepolia",
        "verify": "hardhat verify --network mantleSepolia"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^16.3.1"
    }
}